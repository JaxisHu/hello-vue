
export default {
  /**
   * 过滤接口返回的 key-value 键值对的字典
   * @param key
   * @param opts
   */
  // keyValueFilter(key: any, opts: any) {
  //   let valueOpt: any = opts.filter((x: any) => { return x.key == key; });
  //   return valueOpt.length > 0 ? valueOpt[0].value : "";
  // },
  //
  // parseCo(code: string, companies: any) {
  //   let valueOpt: any = companies.filter((x: any) => { return x.code == code; });
  //   return valueOpt.length > 0 ? valueOpt[0].value : "";
  // }

};
